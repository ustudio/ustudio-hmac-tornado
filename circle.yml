machine:
  python:
    version: 3.6.0
dependencies:
  pre:
    - pip install -r dev-requirements.txt
test:
  override:
    - nosetests
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: ustudio
    commands:
      - ./publish_to_pypi.sh
